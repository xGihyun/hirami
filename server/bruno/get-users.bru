meta {
  name: get-users
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/users
  body: none
  auth: inherit
}

vars:post-response {
  requestedBy: {{res.body.data.id}}
  reviewedBy: {{res.body.data.id}}
}

settings {
  encodeUrl: true
  timeout: 0
}
