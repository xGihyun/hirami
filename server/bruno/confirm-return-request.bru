meta {
  name: confirm-return-request
  type: http
  seq: 16
}

patch {
  url: {{baseUrl}}/return-requests/{{returnRequestId}}
  body: json
  auth: inherit
}

body:json {
  {
    "returnRequestId": "{{returnRequestId}}",
    "reviewedBy": "{{userId}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
