meta {
  name: get-session
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/sessions?token={{sessionToken}}
  body: none
  auth: inherit
}

params:query {
  token: {{sessionToken}}
}

settings {
  encodeUrl: true
}
