meta {
  name: review-borrow-request
  type: http
  seq: 11
}

patch {
  url: {{baseUrl}}/review-borrow-requests
  body: json
  auth: inherit
}

body:json {
  {
    "ids": [
      "{{createdBorrowRequest.equipments[0].borrowRequestId}}",
      "{{createdBorrowRequest.equipments[1].borrowRequestId}}"
    ],
    "reviewedBy": "{{reviewedBy}}",
    "remarks": null,
    "status": "approved"
  }
}

settings {
  encodeUrl: true
}
