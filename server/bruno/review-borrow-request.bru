meta {
  name: review-borrow-request
  type: http
  seq: 9
}

patch {
  url: {{baseUrl}}/borrow-requests/{{borrowRequestId}}
  body: json
  auth: inherit
}

body:json {
  {
    "id": "{{borrowRequestId}}",
    "reviewedBy": "{{reviewedBy}}",
    "remarks": null,
    "status": "approved"
  }
}

settings {
  encodeUrl: true
}
