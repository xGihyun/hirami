meta {
  name: get-borrow-history copy
  type: http
  seq: 24
}

get {
  url: {{baseUrl}}/borrow-history
  body: none
  auth: inherit
}

vars:post-response {
  borrowHistory: {{res.body.data}}
}

settings {
  encodeUrl: true
  timeout: 0
}
