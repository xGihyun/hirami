meta {
  name: get-user
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/users/{{userId}}
  body: none
  auth: inherit
}

vars:post-response {
  requestedBy: {{res.body.data.id}}
  reviewedBy: {{res.body.data.id}}
}

settings {
  encodeUrl: true
}
