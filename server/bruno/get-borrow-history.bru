meta {
  name: get-borrow-history
  type: http
  seq: 20
}

get {
  url: {{baseUrl}}/borrow-history
  body: none
  auth: inherit
}

vars:post-response {
  borrowHistory: {{res.body.data}}
}

settings {
  encodeUrl: true
  timeout: 0
}
