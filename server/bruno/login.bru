meta {
  name: login
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "test@test.com",
    "password": "password"
  }
}

vars:post-response {
  userId: {{res.body.data.user.id}}
}

settings {
  encodeUrl: true
}
