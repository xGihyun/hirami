meta {
  name: create-borrow-request
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/borrow-requests
  body: json
  auth: inherit
}

body:json {
  {
    "equipmentTypeId": "{{equipmentTypeId}}",
    "quantity": 1,
    "location": "Volleyball Court",
    "purpose": "PE Class",
    "expectedReturnAt": "2025-09-26T03:19:44.380Z",
    "requestedBy": "{{requestedBy}}"
  }
}

settings {
  encodeUrl: true
}
