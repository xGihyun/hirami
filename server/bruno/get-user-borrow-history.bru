meta {
  name: get-user-borrow-history
  type: http
  seq: 18
}

get {
  url: {{baseUrl}}/borrow-history
  body: none
  auth: inherit
}

params:query {
  ~userId: {{userId}}
}

settings {
  encodeUrl: true
}
