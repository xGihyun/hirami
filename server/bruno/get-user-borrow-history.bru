meta {
  name: get-user-borrow-history
  type: http
  seq: 16
}

get {
  url: {{baseUrl}}/borrow-history?userId={{userId}}
  body: none
  auth: inherit
}

params:query {
  userId: {{userId}}
}

settings {
  encodeUrl: true
}
